services:
  error-tracker:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_PATH=/app/data/errors.db
    volumes:
      - data:/app/data
    restart: unless-stopped

volumes:
  data:
